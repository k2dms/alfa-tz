## 📦 Kickstart установка
**`alfa-tz/ks/ks.cfg`**  
Kickstart-файл для автоматической установки **CentOS Stream 8** 
Создаёт разделы LVM (`vgSYSTEM`, `vgDATA`), включает сервисы `sshd`, `NetworkManager`, `chronyd`, устанавливает минимальный набор пакетов (`@core`, `lvm2`).

---

## ⚙️ Ansible структура

**`alfa-tz/ansible/main.yml`**  
Главный плейбук — запускает роли `init_os` и `docker_install` на хостах группы `target`.

**`alfa-tz/ansible/hosts.ini`**  
Инвентори-файл.  
Содержит хост `192.168.15.237` с доступом по SSH-ключу и привилегиями `root`.